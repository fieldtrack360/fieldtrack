# The lint jar is packaged whole into every AAR via `lintPublish`, and AGP accepts exactly
# one jar in that configuration. The Kotlin plugin would otherwise add kotlin-stdlib as a
# second runtime dependency and the publish task fails with "Found more than one jar".
# The lint runtime already supplies the stdlib, so it is compileOnly here instead.
kotlin.stdlib.default.dependency=false
